import numpy as np

for _ in range(10) :
    Sum = 0 
    Count = 0 
    while Sum < 1:
        x = np.random.uniform(0,1)
        Sum += x
        Count += 1
    print (np.array(Count), end = " ")

y = np.array()
print("******")
print(y)
y.mean()
print(y.mean())